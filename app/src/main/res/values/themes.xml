<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Win11Launcher" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Applied to MainActivity before Compose content is set. Keeps the
         system window transparent so the first Compose frame (which paints
         its own Mica-style background) doesn't flash a white/black frame. -->
    <style name="Theme.Win11Launcher.Launcher" parent="Theme.Win11Launcher">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen" tools:targetApi="29">false</item>
    </style>

</resources>
